# Rust
/target
/zed-adapter/target

# Frontend
web/node_modules/
tools/do-memory-mock/node_modules/
web/dist/
dist/providers/
dist/provider-runtime-cache/
*.tsbuildinfo

# Nix
/result
/result-*

# Local / runtime
.env
.env.*
*.log
.DS_Store
.wrangler/
**/.wrangler/

# sccache local cache (if ever placed in-tree)
.sccache/

# Materialized at build from the atlantis components package.
web/src/_shell

# web/src/_sync is VENDORED (a committed copy of @shared-utils/sync), not
# materialized — so it's tracked, not ignored. See web/src/_sync/mod.ts.
