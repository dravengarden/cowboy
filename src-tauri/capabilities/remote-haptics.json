{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "remote-haptics",
  "description": "Allow the remote Cowboy UI (loaded over the Stormbird overlay from cowboy.stormbird.xyz) to invoke native OS haptics and open rendered markdown links through the OS. The remote origin needs its own permissions because it is independent of the local loader capability.",
  "windows": ["main"],
  "remote": {
    "urls": [
      "https://cowboy.stormbird.xyz",
      "https://cowboy.stormbird.xyz/*"
    ]
  },
  "permissions": [
    "core:default",
    "opener:allow-open-url",
    "haptics:allow-impact-feedback",
    "haptics:allow-notification-feedback",
    "haptics:allow-selection-feedback",
    "haptics:allow-vibrate"
  ]
}
