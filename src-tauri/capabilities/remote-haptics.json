{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "remote-haptics",
  "description": "Allow the remote cowboy UI (loaded over the tailnet from cowboy.hawk.thundersparrow.top) to invoke native OS haptics and open rendered markdown links through the OS. The remote origin needs its OWN permissions because it is independent of the local loader capability.",
  "windows": ["main"],
  "remote": {
    "urls": [
      "https://cowboy.hawk.thundersparrow.top",
      "https://cowboy.hawk.thundersparrow.top/*"
    ]
  },
  "permissions": [
    "core:default",
    "opener:allow-open-url",
    "haptics:allow-impact-feedback",
    "haptics:allow-notification-feedback",
    "haptics:allow-selection-feedback",
    "haptics:allow-vibrate"
  ]
}
